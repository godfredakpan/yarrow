# API base URL for Her Health Hub (events & bookings).
# Point to your rvw/api instance, e.g. http://localhost:8000/api
# If unset, the app uses /api (same-origin).
# VITE_API_BASE_URL=http://localhost:8000/api
